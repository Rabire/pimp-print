import { Field } from "@/utils/field";

const FIELDS: Field[] = [
  {
    name: "fullName",
    label: "Nom complet",
    type: "text",
    defaultValue: "Goerge DUCOUEDIC",
  },
];

export default FIELDS;
